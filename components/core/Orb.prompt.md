Voice-agent orb; main orb full color with optional call button, neighbours dim. Live state adds breathing + ripple rings.

```jsx
<Orb src="assets/orb-prism.webp" size={64} dim />
<Orb src="assets/orb-spray.webp" size={104} live call />
```